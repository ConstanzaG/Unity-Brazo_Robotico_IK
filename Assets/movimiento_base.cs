using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class movimiento_base : MonoBehaviour
{
    public float speed = 0.1f;
    public float grados_base = 0f;

    void Start()
    {
        Vector3 position = transform.position;
        print("La posición es: " + position);


    }
    void Update()
    {
        // Girar la base con grados
        transform.rotation = Quaternion.Euler(0, grados_base, 0);
    }
}
*/